export const metadata = {
  title: 'Articles | DevFun Blog',
  description: 'Read our latest articles on web development, programming, and technology.',
};

export default function ArticleLayout({ children }) {
  return children;
}